{
  "name": "fruit-market-express-handlebars-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.3",
    "express-handlebars": "^3.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js -q",
    "bsync": "npm run dev & browser-sync start --port 4000 --proxy 'localhost:3000' --files 'src, public' --no-notify"
  },
  "devDependencies": {
    "browser-sync": "^2.18.12"
  }
}
